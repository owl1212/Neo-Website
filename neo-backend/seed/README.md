# seed/

Placeholder — not part of the current scope.

The original tree included this for importing `data/*.ts` content
(products/resellers/news) into Postgres, but that content is managed via
Directus, not this API. Leaving this empty until/unless that changes.
